import css from './LoginFormSection.module.css';
const LoginFormSection = () => {
  return (
    <section className={css.loginFormSection}>
      <h2>Expand your mind, reading a book</h2>
    </section>
  );
};
export default LoginFormSection;
